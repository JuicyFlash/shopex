.sub-product.mx-2
  = link_to  cart_sub_product_path(product: {product_id: cart_product.product_id, quantity: '1'}), id: "sub-cart-product-#{cart_product.id}", data: { turbo_method: :patch }, class: "btn btn-outline-dark btn-sm"
    = bi_icon('dash-circle')